language: haxe

os:
  - linux

haxe:
  - "3.4.4"
  - development

matrix:
  allow_failures:
    - haxe: development

branches:
  only:
    - master
    - dev

script:
  - haxelib install hxnodejs
  - haxelib dev atom .
  - haxe run.hxml
  - neko run.n
  - haxe doc.hxml
